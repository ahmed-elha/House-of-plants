function MyButton ({name, color }) {

  return(
    <button > {name} {color}
    </button>
  
    //   <button > {butpops.name} {butpops.color}
  //  </button>
//     <button style={{backgroundColor: color}}>{name}  {color }
// {console.log(name)}
//       </button>
  // button_name.map((name, index) => <button key={index}>{name}</button>)
  )     
   

    
}
export default MyButton;